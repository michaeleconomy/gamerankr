- @title = "#{@user.real_name}'s Followers"
.breadCrumbs
  = link_to "Users", users_path
%h1
  = link_to_ar @user
  &gt;
  Followers
.contentBlock
  - if @followers.empty?
    No followers yet.
  - else
    = render_ar @followers, locals: {expanded: true}
    = will_paginate @followers