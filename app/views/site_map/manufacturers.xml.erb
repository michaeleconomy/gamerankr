<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <% @manufacturers.each do |id, title| %>
    <url><loc><%= manufacturer_url("#{id}-#{title.gsub(/[^\w]/, '-')}") %></loc></url>
  <% end %>
</urlset>